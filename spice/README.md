# SPICE / LTspice workflow

Status: TRL 2. Official macromodels stay in the local LTspice library.

Classification of runner output after a real LTspice batch: **SPICE/front-end
simulation**. Never laboratory measurement. Never S14 compliance.

Do not commit Analog Devices `.sub` / example `.asc` files.

See [adcmp580/README.md](adcmp580/README.md) and
`python scripts/ltspice/run_adcmp580.py`.
