# Simulation outputs

Generated CSV, JSON, PNG, SVG, and interpretation files are written here by
`python -m tidl_poc sim`. They are **model-based simulation** unless a file
says otherwise. They are not physical measurements.

All generated artefacts except this README are gitignored. CI must not commit
them.
